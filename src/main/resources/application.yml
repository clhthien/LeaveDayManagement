spring:
  application:
    name=LeaveDayManagement
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: tom_dev
    password: 123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update

server:
  port: 8080
  servlet:
    context-path: /day-management